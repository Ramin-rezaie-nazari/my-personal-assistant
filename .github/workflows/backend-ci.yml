name: Backend CI

on:
  push:
    branches:
      - main
      - agent/mypa-autonomous-control-plane
  pull_request:
    branches: [main]
    paths:
      - 'apps/backend/**'
      - 'data/mypa-sports-catalog-expanded/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/backend-ci.yml'

jobs:
  backend:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: personal_assistant
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d personal_assistant"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/personal_assistant?schema=public
      APP_NAME: My Personal Assistant API
      NODE_ENV: test
      JWT_ACCESS_SECRET: test-access-secret
      JWT_REFRESH_SECRET: test-refresh-secret
      JWT_ACCESS_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 30d
      FITNESS_EXPECTED_TOTAL: 919

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.18.0
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate Prisma schema
        run: pnpm --filter backend exec prisma validate

      - name: Generate Prisma client
        run: pnpm --filter backend exec prisma generate

      - name: Apply database migrations
        run: pnpm --filter backend exec prisma migrate deploy

      - name: Verify migrations are fully applied and idempotent
        run: |
          pnpm --filter backend exec prisma migrate status
          pnpm --filter backend exec prisma migrate deploy

      - name: Reconcile committed sports catalog summaries
        run: pnpm --filter backend fitness:catalog:reconcile

      - name: Build backend
        run: pnpm --filter backend build

      - name: Run backend unit tests
        run: pnpm --filter backend test --runInBand

      - name: Run backend API e2e tests
        run: pnpm --filter backend test:e2e

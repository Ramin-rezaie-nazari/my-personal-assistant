#!/usr/bin/env node
import { createHash } from 'node:crypto';
import { access, mkdir, rename, writeFile } from 'node:fs/promises';
import path from 'node:path';
import sharp from 'sharp';

const ROOT = path.resolve(process.env.MYPA_CONTENT_MIRROR_ROOT ?? path.join(process.cwd(), 'content-mirror'));
const MEDIA_ROOT = path.join(ROOT, 'media', 'recipes');
const MANIFEST_PATH = path.join(ROOT, 'recipe-final-manifest.json');
const SUMMARY_PATH = path.join(ROOT, 'recipe-final-summary.json');
const DATASET_URL = process.env.RECIPE_DATASET_URL ?? 'https://huggingface.co/datasets/gossminn/wikibooks-cookbook/resolve/main/recipes_parsed.json?download=true';
const WIKIBOOKS_API = 'https://en.wikibooks.org/w/api.php';
const COMMONS_API = 'https://commons.wikimedia.org/w/api.php';
const MAX_BYTES = 64 * 1024;
const CONCURRENCY = Math.min(12, Math.max(1, Number(process.env.RECIPE_FINAL_CONCURRENCY ?? 8)));
const TIMEOUT_MS = 15_000;
const MAX_ITEMS = Number.isFinite(Number(process.env.RECIPE_FINAL_MAX)) && Number(process.env.RECIPE_FINAL_MAX) > 0 ? Math.floor(Number(process.env.RECIPE_FINAL_MAX)) : null;
const UA = 'MYPA-recipe-final-media/3.0';
const clean = (v='') => String(v).replace(/<[^>]*>/g,' ').replace(/&nbsp;/gi,' ').replace(/&amp;/gi,'&').replace(/&quot;/gi,'\"').replace(/&#39;|&apos;/gi,"'").replace(/\s+/g,' ').trim();
const slug = (v) => clean(v).toLowerCase().normalize('NFKD').replace(/[^a-z0-9\s-]/g,'').replace(/\s+/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'') || createHash('sha1').update(clean(v)).digest('hex').slice(0,12);
const sha256 = (b) => createHash('sha256').update(b).digest('hex');
async function exists(f){try{await access(f);return true}catch{return false}}
async function saveJson(file,value){await mkdir(path.dirname(file),{recursive:true});const tmp=`${file}.tmp-${process.pid}-${Date.now()}-${Math.random().toString(16).slice(2)}`;await writeFile(tmp,JSON.stringify(value,null,2)+'\n','utf8');await rename(tmp,file)}
async function fetchBytes(url,accept='*/*'){let last;for(let i=0;i<4;i++){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);try{const r=await fetch(url,{headers:{'User-Agent':UA,Accept:accept},redirect:'follow',signal:controller.signal});if(!r.ok)throw new Error(`${r.status} ${r.statusText}`);return Buffer.from(await r.arrayBuffer())}catch(e){last=e}finally{clearTimeout(timer)}}throw last}
async function json(url){return JSON.parse((await fetchBytes(url,'application/json')).toString('utf8'))}
function license(meta={}){const text=`${clean(meta.LicenseShortName?.value??meta.LicenseShortName??'')} ${clean(meta.UsageTerms?.value??meta.UsageTerms??'')}`;if(/non[- ]?commercial|\bNC\b|no derivatives|\bND\b/i.test(text))return null;if(/CC0|public domain|public-domain|PDM/i.test(text))return 'CC0/Public Domain';if(/CC BY-SA/i.test(text))return 'CC BY-SA';if(/CC BY/i.test(text))return 'CC BY';return null}
function score(title,text){const wanted=clean(title).toLowerCase().split(/\s+/).filter(x=>x.length>=4);const hay=clean(text).toLowerCase();let s=wanted.reduce((n,t)=>n+(hay.includes(t)?2:0),0);if(/finished|served|plated|dish|meal|prepared|cooked/i.test(hay))s+=6;if(/food|recipe|cookbook/i.test(hay))s+=2;if(/raw ingredient|uncooked|ingredient list/i.test(hay))s-=5;return s}
async function candidatesFromPage(item){let page=`Cookbook:${item.title.replace(/ /g,'_')}`;try{const u=new URL(item.sourceUrl);const p=decodeURIComponent(u.pathname.split('/wiki/')[1]||'');if(p)page=p}catch{}const p=new URLSearchParams({action:'parse',page,prop:'images',format:'json',formatversion:'2'});const names=(await json(`${WIKIBOOKS_API}?${p}`))?.parse?.images??[];const usable=names.filter(n=>/\.(jpe?g|png|webp)$/i.test(n)&&!/icon|logo|flag|difficulty|step|process|instruction/i.test(n)).slice(0,30);if(!usable.length)return[];const q=new URLSearchParams({action:'query',titles:usable.map(n=>`File:${n}`).join('|'),prop:'imageinfo',iiprop:'url|mime|size|extmetadata',iiurlwidth:'1600',format:'json',formatversion:'2'});const pages=(await json(`${WIKIBOOKS_API}?${q}`))?.query?.pages??[];return pages.map(page=>{const i=page?.imageinfo?.[0];if(!i||!/^(image\/(jpeg|png|webp))$/i.test(i.mime??''))return null;if(Number(i.width||0)<500||Number(i.height||0)<500)return null;const lic=license(i.extmetadata??{});if(!lic)return null;const txt=clean(`${page.title??''} ${i.extmetadata?.ObjectName?.value??''} ${i.extmetadata?.ImageDescription?.value??''}`);return{url:i.thumburl||i.url,sourceUrl:i.descriptionurl||i.url,provider:'Wikimedia Commons/Wikibooks',license:lic,attribution:clean(i.extmetadata?.Artist?.value||i.extmetadata?.Credit?.value||'Wikimedia contributor'),score:score(item.title,txt)}}).filter(Boolean).sort((a,b)=>b.score-a.score)}
async function candidatesFromCommons(item){const p=new URLSearchParams({action:'query',generator:'search',gsrnamespace:'6',gsrsearch:`${item.title} finished dish`,gsrlimit:'25',prop:'imageinfo',iiprop:'url|mime|size|extmetadata',iiurlwidth:'1600',format:'json',formatversion:'2'});const pages=(await json(`${COMMONS_API}?${p}`))?.query?.pages??[];return pages.map(page=>{const i=page?.imageinfo?.[0];if(!i||!/^(image\/(jpeg|png|webp))$/i.test(i.mime??''))return null;if(Number(i.width||0)<500||Number(i.height||0)<500)return null;const lic=license(i.extmetadata??{});if(!lic)return null;const txt=clean(`${page.title??''} ${i.extmetadata?.ImageDescription?.value??''} ${i.extmetadata?.Categories?.value??''}`);return{url:i.thumburl||i.url,sourceUrl:i.descriptionurl||i.url,provider:'Wikimedia Commons',license:lic,attribution:clean(i.extmetadata?.Artist?.value||i.extmetadata?.Credit?.value||'Wikimedia contributor'),score:score(item.title,txt)}}).filter(Boolean).sort((a,b)=>b.score-a.score)}
async function encodeWebp(input){const attempts=[[1400,84],[1200,78],[1000,70],[900,62],[800,56],[700,50],[600,44],[500,38],[420,32],[360,28]];let best=null;for(const[w,q]of attempts){const out=await sharp(input,{failOn:'none'}).rotate().resize({width:w,height:w,fit:'inside',withoutEnlargement:true}).webp({quality:q,effort:5}).toBuffer();best=out;if(out.length<=MAX_BYTES)break}if(!best||best.length>MAX_BYTES)throw new Error('WebP size budget exceeded');return best}
function fallbackSvg(item){const safe=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');const ing=item.ingredients.slice(0,6).join(' • ');return `<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="900" viewBox="0 0 1200 900"><defs><radialGradient id="bg"><stop offset="0" stop-color="#fff"/><stop offset="1" stop-color="#e8ded0"/></radialGradient><radialGradient id="food"><stop offset="0" stop-color="#eaa96a"/><stop offset="1" stop-color="#a84f2e"/></radialGradient></defs><rect width="1200" height="900" fill="url(#bg)"/><ellipse cx="600" cy="575" rx="390" ry="165" fill="#b8a18a"/><ellipse cx="600" cy="540" rx="340" ry="145" fill="#fff"/><ellipse cx="600" cy="535" rx="260" ry="105" fill="url(#food)"/><circle cx="505" cy="515" r="48" fill="#79a84f"/><circle cx="650" cy="545" r="55" fill="#d3a33f"/><circle cx="715" cy="500" r="43" fill="#c75e4e"/><text x="600" y="115" text-anchor="middle" font-family="Arial" font-size="54" font-weight="700" fill="#342d27">${safe(item.title.slice(0,36))}</text><text x="600" y="190" text-anchor="middle" font-family="Arial" font-size="25" fill="#63564a">Finished-dish image generated by MYPA</text><text x="600" y="790" text-anchor="middle" font-family="Arial" font-size="20" fill="#63564a">${safe(ing.slice(0,125))}</text><text x="600" y="835" text-anchor="middle" font-family="Arial" font-size="17" fill="#7c6b5a">Original MYPA fallback when no verified licensed finished-dish photo is available</text></svg>`}
function parse(row){const d=row?.recipe_data??row??{};const lines=Array.isArray(d.text_lines)?d.text_lines:[];const title=clean(d.title??row.title??row.filename?.split('/').pop()?.replace(/\.html$/i,'')??'Untitled Recipe');const ingredients=lines.filter(x=>x?.line_type==='ul'&&/ingredient/i.test(x.section??'')).map(x=>clean(x.text)).filter(Boolean);return{title,sourceUrl:d.url??`https://en.wikibooks.org/wiki/Cookbook:${encodeURIComponent(title.replace(/ /g,'_'))}`,ingredients}}
async function build(item){const dir=path.join(MEDIA_ROOT,slug(item.title));await mkdir(dir,{recursive:true});let candidates=[];const results=await Promise.allSettled([candidatesFromPage(item),candidatesFromCommons(item)]);for(const r of results)if(r.status==='fulfilled')candidates.push(...r.value);const seen=new Set();candidates=candidates.filter(c=>{if(seen.has(c.sourceUrl))return false;seen.add(c.sourceUrl);return true}).sort((a,b)=>b.score-a.score);for(const c of candidates){try{const input=await fetchBytes(c.url,'image/*,*/*;q=0.8');const webp=await encodeWebp(input);const digest=sha256(webp);const file=path.join(dir,`final-${digest.slice(0,12)}.webp`);await writeFile(file,webp);return{kind:'recipe',name:item.title,slug:slug(item.title),sourceUrl:item.sourceUrl,stage:'final',status:'ready',mediaCount:1,format:'webp',sizeBytes:webp.length,sha256:digest,localPath:path.relative(ROOT,file).split(path.sep).join('/'),objectKey:path.relative(MEDIA_ROOT,file).split(path.sep).join('/'),provider:c.provider,license:c.license,attribution:c.attribution,sourceMediaUrl:c.sourceUrl,sourceType:'licensed-real',generated:false}}catch{}}
const webp=await encodeWebp(Buffer.from(fallbackSvg(item)));const file=path.join(dir,'final-generated.webp');await writeFile(file,webp);return{kind:'recipe',name:item.title,slug:slug(item.title),sourceUrl:item.sourceUrl,stage:'final',status:'ready',mediaCount:1,format:'webp',sizeBytes:webp.length,sha256:sha256(webp),localPath:path.relative(ROOT,file).split(path.sep).join('/'),objectKey:path.relative(MEDIA_ROOT,file).split(path.sep).join('/'),provider:'MYPA deterministic renderer',license:'generated-original',attribution:'MYPA',sourceType:'recipe-derived-generated',generated:true,basedOnRecipe:true}}
async function main(){await mkdir(MEDIA_ROOT,{recursive:true});const dataset=await json(DATASET_URL);if(!Array.isArray(dataset))throw new Error('Recipe dataset invalid');const rows=MAX_ITEMS?dataset.slice(0,MAX_ITEMS):dataset;const manifest={schemaVersion:3,generatedAt:null,root:ROOT,requiredMediaPerRecipe:1,items:{}};let cursor=0;const worker=async()=>{while(true){const idx=cursor++;if(idx>=rows.length)return;const item=parse(rows[idx]);try{manifest.items[`recipe:${slug(item.title)}`]=await build(item)}catch(e){manifest.items[`recipe:${slug(item.title)}`]={kind:'recipe',name:item.title,slug:slug(item.title),sourceUrl:item.sourceUrl,status:'failed',mediaCount:0,error:e instanceof Error?e.message:String(e)}}}};await Promise.all(Array.from({length:CONCURRENCY},worker));manifest.generatedAt=new Date().toISOString();await saveJson(MANIFEST_PATH,manifest);const items=Object.values(manifest.items);const complete=items.filter(x=>x.status==='ready'&&x.mediaCount===1&&x.format==='webp'&&x.sizeBytes>0&&x.sizeBytes<=MAX_BYTES).length;const summary={schemaVersion:3,generatedAt:manifest.generatedAt,root:ROOT,requiredMediaPerRecipe:1,recipes:items.length,complete,incomplete:items.length-complete,failed:items.filter(x=>x.status==='failed').length,licensed:items.filter(x=>x.generated===false).length,generated:items.filter(x=>x.generated===true).length,maxBytes:MAX_BYTES,corpusComplete:items.length>0&&complete===items.length};await saveJson(SUMMARY_PATH,summary);console.log(JSON.stringify(summary,null,2));if(!summary.corpusComplete)process.exitCode=2}
main().catch(e=>{console.error(e);process.exitCode=1})

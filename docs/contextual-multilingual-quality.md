# Contextual Multilingual Quality Contract

This contract certifies that the contextual command layer recognizes previous-action references across the 51 registered voice locales and binds follow-up updates to the stored prior action without attaching standalone new commands to stale context.

The automated suite is `apps/backend/src/modules/assistant/services/contextual-multilingual-quality.spec.ts`.

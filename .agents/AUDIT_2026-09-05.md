# MYPA Agent Audit — 2026-09-05

## Verified repository facts

- Default branch: `main`.
- Monorepo contains `apps/backend` and `apps/mobile`.
- Authoritative project-state documents are under `apps/backend/docs/`.
- `apps/backend/docs/05_CURRENT_STATE.md` reports approximately 65% weighted completion and records the last fully validated local checkpoint as 2026-08-18.
- The current-state document says the Food Decision Brain is implemented but not yet fully validated.

## Drift findings

### Recommendation Intelligence
On `main`:
- `apps/backend/src/modules/recommendation-intelligence/services/recommendation-engine.service.ts` is a stub returning an empty recommendation array.
- `apps/backend/src/modules/recommendation-intelligence/services/personalization.service.ts` is a stub returning `personalized: true`.
- `apps/backend/src/modules/recommendation-intelligence/controllers/recommendation-intelligence.controller.ts` is an empty controller.
- `apps/backend/src/app.module.ts` does not import `RecommendationIntelligenceModule`.

Meanwhile, the actual working deterministic recommendation path exists in `apps/backend/src/modules/recipes/services/food-operating-loop.service.ts` and is exposed by `RecipesController` at `/recipes/recommendations`.

Conclusion: the dedicated Recommendation Intelligence slice described in current-state is not wired into `main` as claimed. This must be resolved before the state can honestly be marked green.

### Voice
The repository contains a dedicated voice-stability branch and the docs describe TTS hardening work. Physical Android validation remains a separate gate.

### Global market
Current-state documents say stacked Global Market / Price Intelligence work is not yet on `main` and requires conflict/dependency review before integration.

## Immediate agent strategy

1. Treat Recommendation Intelligence drift as a P1 source-of-truth issue.
2. Reuse the existing canonical Food Operating Loop instead of creating a parallel recommendation engine by default.
3. Resolve the voice native-crash blocker with device evidence before release claims.
4. Integrate global market work only after conflict and regression analysis.
5. Continue through mobile, production-hardening and release gates with evidence-based status.

## Evidence policy

No milestone is green solely because documentation says it is green. Code, tests, CI/device evidence and release checks must agree.
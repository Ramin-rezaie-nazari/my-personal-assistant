# Copy this file to apps/backend/.env for local development.
DATABASE_URL=postgresql://mypa:mypa-local-only@localhost:5432/mypa

# Development-only placeholders; replace locally as required by config validation.
JWT_SECRET=change-me-local-only
JWT_REFRESH_SECRET=change-me-local-only-refresh
NODE_ENV=development
PORT=3000
